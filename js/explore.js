

var word = "hello";
function reverse(str) {
 	var backwards = "";
 	for (var i = str.length - 1;i >= 0; i--) {
 	backwards += str[i];
   }
   return backwards
 }

 var reversed = reverse(word);
 if (1 == 1) {
   console.log(reversed);

  }